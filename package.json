{
  "name": "pranavdhawan-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:html": "html-validate index.html blog/*.html",
    "test:function": "node --test tests/ask-function.node.mjs tests/knowledge-sync.node.mjs",
    "check:headers": "node tests/check-netlify-headers.mjs",
    "check": "npm run test:html && npm run check:headers && npm run test:function && npm test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.54.2",
    "html-validate": "^9.5.3"
  }
}
